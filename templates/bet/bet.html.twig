 {% extends 'base.html.twig' %}
 {% block body %}

  {% if isOk %}
  <h1>votre pari a bien été </h1>


  {% else %}

 <h1>{{ "Faites vos jeux !" }}</h1>

  {{ form_start(BetForm) }}
  {{ form_rest(BetForm) }}
  <button type="submit" class="btn btn-outline-info">Valider mon pari</button>
  {{ form_end(BetForm) }}

  {% endif %}


 {% endblock %}